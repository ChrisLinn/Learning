DATAS SEGMENT
    X_NUM db 42
	Y_NUM db -43
	S_NUM db ?
DATAS ENDS

STACKS SEGMENT
    STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
	mov al,X_NUM
	add al,Y_NUM
	MOV S_NUM,AL
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
