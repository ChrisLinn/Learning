DATAS SEGMENT
    INBUFMAX DB 32
    INBUFACT DB ?
    INBUF    DB 32  DUP(?)
    OUTBUF DB 'HelloAsm',0DH,0AH,'$'
			
DATAS ENDS
CODES SEGMENT
	ASSUME CS:CODES,DS:DATAS
START:MOV AX,DATAS
	MOV DS,AX
	;CALL FUN1
	;CALL FUN2
	;CALL FUN3
	CALL FUN4
    MOV AH,4CH
    INT 21H
FUN1 PROC NEAR
	MOV AH,01H
	INT 21H
	RET
FUN1 ENDP
FUN2 PROC NEAR

	MOV DL,0AH
	MOV AH,02H
	INT 21H
	MOV DL,0DH
	MOV AH,02H
	INT 21H
	
    MOV DL,OUTBUF
    MOV AH,02H
	INT 21H
    RET
FUN2 ENDP
FUN3 PROC NEAR

	MOV DL,0AH
	MOV AH,02H
	INT 21H
	MOV DL,0DH
	MOV AH,02H
	INT 21H
	
    MOV DX,OFFSET OUTBUF
    MOV AH,09H
    INT 21H
    RET
FUN3 ENDP
FUN4 PROC NEAR
    MOV DX,OFFSET INBUFMAX
    MOV AH,0AH
    INT 21H
	
	MOV DL,0AH
	MOV AH,02H
	INT 21H
	MOV DL,0DH
	MOV AH,02H
	INT 21H
	
	;MOV DL,0AH
	;MOV AH,02H
	;INT 21H
	;MOV DL,0DH
	;MOV AH,02H
	;INT 21H
	
	MOV DX,OFFSET INBUF
	MOV AH,09H
    INT 21H
	
    RET
FUN4 ENDP
CODES ENDS
     END START 